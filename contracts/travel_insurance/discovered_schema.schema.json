{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://capstone.local/contracts/travel-insurance/discovered-schema.schema.json",
  "title": "Discovered Australian travel insurance extraction schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "vertical",
    "version",
    "description",
    "product_types",
    "fields",
    "coverage_categories",
    "notes"
  ],
  "properties": {
    "vertical": {"const": "travel_insurance"},
    "version": {"type": "string", "minLength": 1},
    "description": {"type": "string", "minLength": 1},
    "product_types": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "enum": [
          "international_single_trip",
          "international_multi_trip",
          "domestic",
          "inbound",
          "business",
          "cruise"
        ]
      }
    },
    "fields": {
      "type": "array",
      "minItems": 1,
      "items": {"$ref": "#/$defs/field"}
    },
    "coverage_categories": {
      "type": "array",
      "items": {"$ref": "#/$defs/canonical_item"}
    },
    "notes": {
      "type": "array",
      "items": {"type": "string"}
    }
  },
  "$defs": {
    "field": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "type",
        "description",
        "applies_to",
        "required",
        "values",
        "aliases"
      ],
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9]*(?:_[a-z0-9]+)*$"
        },
        "type": {
          "enum": ["string", "number", "boolean", "enum", "list[object]"]
        },
        "description": {"type": "string", "minLength": 1},
        "applies_to": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "enum": [
              "international_single_trip",
              "international_multi_trip",
              "domestic",
              "inbound",
              "business",
              "cruise"
            ]
          }
        },
        "required": {"type": "boolean"},
        "values": {
          "type": "array",
          "items": {"type": ["string", "number", "boolean"]}
        },
        "aliases": {
          "type": "array",
          "items": {"type": "string", "minLength": 1}
        }
      }
    },
    "canonical_item": {
      "type": "object",
      "additionalProperties": false,
      "required": ["canonical_name", "description", "aliases"],
      "properties": {
        "canonical_name": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9]*(?:_[a-z0-9]+)*$"
        },
        "description": {"type": "string", "minLength": 1},
        "aliases": {
          "type": "array",
          "items": {"type": "string", "minLength": 1}
        }
      }
    }
  }
}
